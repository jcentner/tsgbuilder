# example: https://<YOUR_RESOURCE>.services.ai.azure.com/api/projects/<YOUR_PROJECT>
PROJECT_ENDPOINT=

# from management center -> project -> connected resources
# example: /subscriptions/<YOUR_SUBSCRIPTION_ID>/resourceGroups/<YOUR_RESOURCE_GROUP>/providers/Microsoft.CognitiveServices/accounts/<YOUR_RESOURCE>/projects/<YOUR_PROJECT>/connections/<YOUR_CONNECTION>
BING_CONNECTION_NAME=

# recommend gpt-5.2 when it is available; need to update everything to use v2 agents
MODEL_DEPLOYMENT_NAME=gpt-4.1

AGENT_NAME=TSG-Builder

# Prompt style: "gpt41" for detailed prompts (recommended for gpt-4.1), "gpt52" for concise prompts
# gpt41: More explicit instructions with mandatory research phases and validation checklists
# gpt52: Concise instructions optimized for GPT-5.2's improved inference capabilities
PROMPT_STYLE=gpt41

# Multi-stage pipeline: "true" (default) uses Research→Write→Review stages for better accuracy
USE_PIPELINE=true
