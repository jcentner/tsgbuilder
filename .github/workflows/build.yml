name: Build Executables

on:
  push:
    tags:
      - 'v*'  # Triggers on version tags like v1.0.0, v1.0.0-beta.1
  workflow_dispatch:  # Manual trigger for testing
    inputs:
      create_release:
        description: 'Create a draft release'
        type: boolean
        default: false

permissions:
  contents: write  # Required for creating releases

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux
            folder_name: tsg-builder-linux
          - os: macos-latest
            platform: macos
            folder_name: tsg-builder-macos
          - os: windows-latest
            platform: windows
            folder_name: tsg-builder-windows

    runs-on: ${{ matrix.os }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Build executable
        run: python build_exe.py
        env:
          APPINSIGHTS_CONNECTION_STRING: ${{ secrets.APPINSIGHTS_CONNECTION_STRING }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: tsg-builder-${{ matrix.platform }}
          path: dist/${{ matrix.folder_name }}/
          if-no-files-found: error

      - name: Build Windows installer
        if: matrix.platform == 'windows'
        run: |
          choco install innosetup -y --no-progress
          # Strip leading 'v' from tag (e.g. v1.0.7 -> 1.0.7) so
          # Add/Remove Programs shows a clean version number.
          $ver = "${{ github.ref_name }}" -replace '^v', ''
          & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer.iss /DAppVersion=$ver
        shell: pwsh

      - name: Upload installer
        if: matrix.platform == 'windows'
        uses: actions/upload-artifact@v4
        with:
          name: tsg-builder-windows-installer
          path: Output/tsg-builder-windows-setup.exe
          if-no-files-found: error

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v') || inputs.create_release
    
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Prepare release files
        run: |
          mkdir -p release
          ls -la artifacts/

      - name: Zip executables
        run: |
          cd artifacts
          # Restore executable permissions lost by upload/download-artifact
          chmod +x tsg-builder-linux/tsg-builder-linux 2>/dev/null || true
          chmod +x tsg-builder-macos/tsg-builder-macos 2>/dev/null || true
          # Zip each platform folder for distribution (skip installer artifact)
          for dir in tsg-builder-linux/ tsg-builder-macos/ tsg-builder-windows/; do
            platform="${dir%/}"
            zipname="${platform}.zip"
            echo "Zipping $dir -> $zipname"
            cd "$dir"
            zip -r "../../release/$zipname" .
            cd ..
          done
          # Zip the installer so browsers don't flag a bare .exe download
          cd tsg-builder-windows-installer
          zip -r "../../release/tsg-builder-windows-setup.zip" tsg-builder-windows-setup.exe
          cd ..
          ls -la ../release/

      - name: Generate SHA256 checksums
        run: |
          cd release
          sha256sum * > SHA256SUMS.txt
          cat SHA256SUMS.txt

      - name: Determine if pre-release
        id: prerelease
        run: |
          if [[ "${{ github.ref_name }}" == *"-"* ]]; then
            echo "is_prerelease=true" >> $GITHUB_OUTPUT
          else
            echo "is_prerelease=false" >> $GITHUB_OUTPUT
          fi

      - name: Create draft release
        uses: softprops/action-gh-release@v2
        with:
          draft: true
          prerelease: ${{ steps.prerelease.outputs.is_prerelease }}
          generate_release_notes: true
          files: |
            release/tsg-builder-linux.zip
            release/tsg-builder-macos.zip
            release/tsg-builder-windows.zip
            release/tsg-builder-windows-setup.zip
            release/SHA256SUMS.txt
          body: |
            ## TSG Builder ${{ github.ref_name }}
            
            ### Downloads
            | Platform | File |
            |----------|------|
            | Linux | `tsg-builder-linux.zip` |
            | macOS | `tsg-builder-macos.zip` |
            | Windows (installer) | `tsg-builder-windows-setup.zip` |
            | Windows (zip) | `tsg-builder-windows.zip` |
            
            ### First Run
            1. Download the zip for your platform and extract
            2. Run the executable inside the extracted folder — a `.env` file will be created automatically
            3. Your browser will open to the setup wizard
            4. Configure your Azure AI Foundry settings
            
            ### Upgrading
            - **Installer (Windows)**: Download and extract `tsg-builder-windows-setup.zip`, then run the installer — your configuration is preserved
            - **Zip (all platforms)**: Extract into the same folder, overwriting existing files
            - If you see an "agents stale" warning, click **Recreate Agents** in Setup
            
            ### Verify Download (optional)
            ```bash
            # Linux/macOS
            sha256sum -c SHA256SUMS.txt
            
            # Windows PowerShell
            Get-FileHash tsg-builder-windows.zip -Algorithm SHA256
            ```
            
            ---
            _See [README](https://github.com/${{ github.repository }}/blob/main/README.md) for full documentation._
